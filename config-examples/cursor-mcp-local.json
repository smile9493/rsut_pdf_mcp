{
  "mcpServers": {
    "pdf-module-local": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "pdf-module-api",
        "pdf-mcp",
        "serve",
        "--transport",
        "stdio"
      ]
    }
  }
}
